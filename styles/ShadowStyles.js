import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        shadowColor: '#171717',
        shadowOffset: {width: 0, height: 5},
        shadowOpacity: 0.3,
        shadowRadius: 3,
    },
})